# config/packages/mailer.yaml
framework:
    mailer:
        transports:
            dsn: '%env(MAILER_DSN)%' # Your existing mailer configuration
            mailtrap: '%env(MAILTRAP_DSN)%' # Custom transport for Mailtrap