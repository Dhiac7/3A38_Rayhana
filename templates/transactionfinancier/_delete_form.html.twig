<form method="post" action="{{ path('app_transactionfinancier_delete', {'id': transactionfinancier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transactionfinancier.id) }}">
    <button class="btn">Delete</button>
</form>
