{% extends 'baseAdmin.html.twig' %}

{% block title %}Transaction - {{ transactionfinancier.id }}{% endblock %}

{% block body %}
<div class="main-panel">
    <div class="content-wrapper">
        <!-- 🟢 Page Header -->
        <div class="page-header">
            <h3 class="page-title">
                <span class="page-title-icon bg-gradient-primary text-white me-2">
                    <i class="mdi mdi-cash"></i>
                </span> Détails de la Transaction Financière
            </h3>
            <nav aria-label="breadcrumb">
                <ul class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_dashboard') }}">Accueil</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Transaction - {{ transactionfinancier.id }}</li>
                </ul>
            </nav>
        </div>

        <div class="row">
            <!-- 📋 Informations Transaction -->
            <div class="col-lg-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Informations de la Transaction</h4>
                        <ul class="list-unstyled">
                            <li><strong>💰 Montant :</strong> {{ transactionfinancier.montant }} €</li>
                            <li><strong>🔄 Type :</strong> {{ transactionfinancier.type }}</li>
                            <li><strong>📅 Date :</strong> {{ transactionfinancier.date|date('Y-m-d H:i:s') }}</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- 📝 Description -->
            <div class="col-lg-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Description</h4>
                        <p>Voici les détails complets de la transaction financière enregistrée. Assurez-vous que toutes les informations sont correctes avant de procéder à des modifications.</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- 🎯 Actions -->
        <div class="row">
            <div class="col-lg-12 text-center mt-4">
                <a href="{{ path('app_transactionfinancier_index') }}" class="btn btn-gradient-primary me-2">
                    <i class="mdi mdi-arrow-left"></i> Retour à la liste
                </a>
                <a href="{{ path('app_transactionfinancier_edit', {'id': transactionfinancier.id}) }}" class="btn btn-gradient-warning">
                    <i class="mdi mdi-pencil"></i> Modifier
                </a>
                {{ include('transactionfinancier/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
