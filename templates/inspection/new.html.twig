{% extends 'baseAdmin.html.twig' %}

{% block title %}New Inspection{% endblock %}

{% block body %}
    <div class="main-panel">
        <div class="content-wrapper">
            <div class="row justify-content-center">
                <div class="col-md-10">
                    <div class="card shadow-lg border-0 rounded-lg">
                        <div class="card-header py-3" style="background-color: #49a760; color: white;">
                            <div class="d-flex justify-content-center align-items-center">
                                <h4 class="card-title mb-0">Create new Inspection</h4>
                            </div>
                        </div>
                        <div class="card-body">
                            {{ include('inspection/_form.html.twig') }}
                        </div>
                        <div class="card-footer text-center">
                            <a href="{{ path('app_inspection_index') }}" class="btn btn-secondary">Back to list</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}