{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Inspection{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-lg p-4">
        <div class="card-header bg-primary text-white text-center">
            <h3>Modifier l'Inspection</h3>
        </div>
        <div class="card-body">
            <h5 class="sub-title text-center">Modifier les informations de l'inspection</h5>
            
            {# Start Symfony Form #}
            {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'contact-form contact-form'}}) }}
                {% for error in form.vars.errors %}
                    <div class="alert alert-danger">{{ error.message }}</div>
                {% endfor %}
                
                <div class="text-center">
                    <button type="submit" class="btn btn-success btn-sm">
                        <i class="mdi mdi-content-save"></i> Enregistrer
                    </button>
                </div>

            {{ form_end(form) }}
            {# End Symfony Form #}
        </div>
    </div>
</div>

<a href="{{ path('app_inspection_index') }}">Retour à la liste</a>

{{ include('inspection/_delete_form.html.twig') }}
{% endblock %}
