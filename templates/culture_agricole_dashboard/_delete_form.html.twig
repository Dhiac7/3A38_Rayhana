<form method="post" action="{{ path('app_culture_agricole_dashboard_delete', {'id': culture_agricole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ culture_agricole.id) }}">
    <button class="btn">Delete</button>
</form>
